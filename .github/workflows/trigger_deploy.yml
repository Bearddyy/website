name: Trigger Automatic Deploy
run-name: ${{ github.actor }} Triggered Deplot 🚀
on:
  push:
    branches:
      - main
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Deploy
        env:
          RENDER_TOKEN: ${{ secrets.RENDER_TOKEN }}
        run: echo "Triggering Deploy to Render"
        run: wget https://api.render.com/deploy/github/$RENDER_TOKEN > /dev/null 